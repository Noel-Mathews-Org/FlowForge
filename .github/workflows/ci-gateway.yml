name: Gateway CI

on:
  push:
    branches:
      - Cloud-Track-dev
    paths:
      - 'gateway/**'

permissions:
  contents: write
  packages: write
  id-token: write

jobs:
  run-ci:
    uses: Noel-Mathews-Org/.github/.github/workflows/_ci-reusable.yml@main
    with:
      service_name: gateway
      project_key: Flow_gateway
    secrets: inherit

