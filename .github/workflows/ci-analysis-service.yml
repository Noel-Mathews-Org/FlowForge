name: Analysis Service CI

on:
  push:
    branches:
      - Cloud-Track-dev
    paths:
      - 'analysis-service/**'

permissions:
  contents: write
  id-token: write

jobs:
  run-ci:
    uses: Noel-Mathews-Org/.github/.github/workflows/_ci-reusable.yml@main
    with:
      service_name: analysis-service
      project_key: Flow_analysis_service
    secrets: inherit

